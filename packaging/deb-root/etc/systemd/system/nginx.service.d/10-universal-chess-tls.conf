[Unit]
# Ensure the hostname-matching TLS certificate is generated before nginx
# starts. Without this ordering nginx could start first and fail (or serve a
# stale cert) when the ssl/ files are missing or issued for an old hostname.
Wants=universal-chess-tls.service
After=universal-chess-tls.service
